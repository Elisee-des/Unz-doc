{% extends 'base.admin.html.twig' %}

{% block title %}
	liste des Administrateurs de unz-doc
{% endblock %}

{% block titre %}
	liste de tous les Administrateurs de unz-doc
{% endblock %}

{% block boutons %}
<a href="{{ path("app_admin_roles_admin_ajout") }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un examen">
		<i class="mdi mdi-arrow-all  "></i>Ajouter un admin
    </a>
	  <a href="{{ path("app_admin_roles_admin_fichier_excel_ajout") }}" class="btn btn-danger align-items-center" title="Cliquez ici pour ajouter un examen">
		<i class="mdi mdi-arrow-all  "></i>Ajouter un fichier excel
    </a>
{% endblock %}

{% block body %}
            <div>
              <p>
                IMPORTANT: <strong>Ici figure la liste des gestionnaire de unz-doc. Ils ont pour de voir de suivre les examens et corrections ajouter, ils ont aussi le pouvoir de banir d'ajouter ou de banir un etudiant.</strong>
              </p>
            </div>
	
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <div class="table-responsive">
                    <table  class="table table">
                      <thead>
                        <tr>
                          <th>Nom Prénom</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                      {% for admin in admins %}
                        <tr>
                          <td><strong>{{ admin.nomPrenom }}</strong></td>
                          <td>
                            <a href="{{ path("app_admin_roles_admin_detail", {"idAdmin": admin.id}) }}" class="btn btn-outline-dark">Détails</a>
                          {# <a href="{{ path("app_admin_filiere_liste", {"idDepartement": departement.id}) }}",  class="btn btn-outline-primary">Filières</a> #}
                          </td>
                        </tr>
                      {% endfor %}
		                  {{ knp_pagination_render(admins) }}

                      </tbody>
                    </table>
                  </div>
                </div>
              </div>

{% endblock %}
