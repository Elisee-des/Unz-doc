{% extends 'base.admin.html.twig' %}

{% block title %}
	Liste des Propositions
{% endblock %}

{% block titre %}
	Liste des Propositions
{% endblock %}

{% block boutons %}
	{# <a href="{{ path("app_admin_departement_ajout") }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un departement">
		<i class="mdi mdi-arrow-all  "></i>Ajouter un departement
    </a> #}
{% endblock %}

{% block body %}
	
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <div class="table-responsive">
                    <table class="table table">
                      <thead>
                        <tr>
                          <th>Propositions</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <tr>
                          <td><strong>Propositions examens (total: {{ PropsExamens|length }}) </strong></td>
                          <td><a href="{{ path("app_admin_proposition_examen_liste") }}" class="btn btn-outline-dark">Ouvir</a></td>
                        </tr>
                        <tr>
                          <td><strong>Propositions corrigés (total: {{ PropsCorrections|length }}) </strong></td>
                          <td><a href="{{ path("app_admin_proposition_correction_liste") }}",  class="btn btn-outline-primary">ouvrir</a></td>
                        </tr>
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>

{% endblock %}
