{% extends 'base.admin.html.twig' %}

{% block title %}
	Liste des licences {{ filiere.nom }}
{% endblock %}

{% block titre %}
	Liste des licences <strong>{{ filiere.nom }}</strong>
{% endblock %}

{% block boutons %}
<a href="{{ path("app_admin_licence_ajout", {"idDepartement": departement.id, "idFiliere": filiere.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un licence">
		<i class="mdi mdi-arrow-all  "></i>Ajouter une licence
    </a>
	<a href="{{ path("app_admin_filiere_liste", {"idDepartement": departement.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un licence">
		<i class="mdi mdi-reply-all  "></i>Retour
    </a>
{% endblock %}

{% block body %}
	
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <div class="table-responsive">
                  {% if licences == NULL %}
                    <h3 class="text-center"><strong>Aucune filière pour le moment.</strong></h3>
                  {% else %}
                  <table class="table table">
                      <thead>
                        <tr>
                          <th>Licences</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                      {% for licence in licences %}
                         <tr>
                          <td><strong>{{ licence.nom }}</strong></td>
                          <td>
                            <a href="{{ path("app_admin_licence_detail", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id}) }}" title="Cliquez ici pour voir les détails cette licence" class="btn btn-outline-dark">Détails</a>
                            <a href="{{ path("app_admin_option_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id}) }}" class="btn btn-outline-primary">Options</a>
                          </td>
                        </tr>
                      {% endfor %}
                      </tbody>
                  {% endif %}
                    </table>
                  </div>
                </div>
              </div>
            </div>

{% endblock %}
