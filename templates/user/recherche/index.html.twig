{% extends 'base.user.html.twig' %}

{% block title %}
	Liste des années universitaire de l'option
{% endblock %}

{% block titre %}
	Liste des années universitaire de l'option
	<strong></strong>
{% endblock %}

{% block description %}
	{# <a href="{{ path("app_user_document_option_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un annee">
				<i class="mdi mdi-reply-all  "></i>Retour
		    </a> #}
{% endblock %}

{% block body %}

	<div class="card">
		<div class="card-header d-flex justify-content-between align-items-center">
			<h4 class="card-title">Effectuer une recherche
			</h4>
			{# <div class="d-flex ">
						      	<a href="{{ path("app_user_document_option_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un annee">
						        <i class="mdi mdi-reply-all  "></i>Retour
						        </a>
									</div> #}
		</div>
		<div class="card-body px-0 pb-0">
			<div class="table-responsive">
				<div class="container">
					{{ form(formRecherche) }}
					</div>
				</div>
			</div>
		</div>

		<h4 class="text-center">Resultat de la recherche</h4>
		<hr>
		<h4 class="text-center">Examens</h4>

		{% if examens == NULL %}
			<h4 class="text-center">
				<strong>Desolé aucun examen trouvé</strong>
			</h4>
		{% else %}
			{% for examen in examens %}
				<div class="card">
					<div class="card-content">
						<div class="card-body">
							<h5 class="card-title">{{examen.nom}}</h5>
						</div>
					</div>
					<ul class="list-group list-group-flush">
						<li class="list-group-item">Nom du module:
							{{ module.nom }}</li>
						<li class="list-group-item">Filière:
							{{filiere.nom}}</li>
						<li class="list-group-item">Année accademique:
							{{annee.nom}}</li>
						<li class="list-group-item">Remarque:
							{% if examen.remarque == '' %}
								<p>Aucune remarque</p>
							{% else %}
								{{examen.remarque}}
							{% endif %}
						</li>
						<li class="list-group-item">Taille du fichier:
							{{examen.taille_fichier}}</li>
						<li class="list-group-item">Date d'ajout:
							{{examen.date_creation}}</li>
						<li class="list-group-item">
							{% if examen.fichier =="aucunFichier" %}
								<p>Aucun fichier</p>
							{% else %}
								<a href="{{ path("app_user_document_examen_telecharger", {"idExamen": examen.id}) }}" class="btn btn-outline-danger">Télecharger l'examen</a>
							{% endif %}
						</li>
					</ul>
				</div>
			{% endfor %}
		{% endif %}

		<hr>
		<h4 class="text-center">Corrections</h4>

		{% if examens == NULL %}
			<h4 class="text-center">
				<strong>Desolé aucune correction trouvé</strong>
			</h4>
		{% else %}
			{% for correction in corrections %}
				<div class="card">
					<div class="card-content">
						<div class="card-body">
							<h5 class="card-title">{{correction.nom}}</h5>
						</div>
					</div>
					<ul class="list-group list-group-flush">
						<li class="list-group-item">Nom du module:
							{{ module.nom }}</li>
						<li class="list-group-item">Filière:
							{{filiere.nom}}</li>
						<li class="list-group-item">Année accademique:
							{{annee.nom}}</li>
						<li class="list-group-item">Remarque:
							{% if correction.remarque == '' %}
								<p>Aucune remarque</p>
							{% else %}
								{{correction.remarque}}
							{% endif %}
						</li>
						<li class="list-group-item">Taille du fichier:
							{{correction.taille_fichier}}</li>
						<li class="list-group-item">Date d'ajout:
							{{correction.date_creation}}</li>
						<li class="list-group-item">
							{% if correction.fichier =="aucunFichier" %}
								<p>Aucun fichier</p>
							{% else %}
								<a href="{{ path("app_user_document_examen_telecharger", {"idExamen": correction.id}) }}" class="btn btn-outline-danger">Télecharger l'examen</a>
							{% endif %}
						</li>
					</ul>
				</div>
			{% endfor %}
		{% endif %}

	{% endblock %}
