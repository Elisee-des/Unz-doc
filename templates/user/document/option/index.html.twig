{% extends 'base.user.html.twig' %}

{% block title %}
	Liste des options de la licence
	{{ licence.nom }}
{% endblock %}

{% block titre %}
	Liste des options de la licence
	<strong>{{ licence.nom }}</strong>
{% endblock %}


{% block description %}
	<a href="{{ path("app_user_document_licence_liste", {"idDepartement": departement.id, "idFiliere": filiere.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un option">
		<i class="mdi mdi-reply-all  "></i>Retour
	</a>
{% endblock %}

{% block body %}

	{# <div class="col-lg-12 grid-margin stretch-card">
		<div class="card">
			<div class="card-body">
				<h4 class="card-title"></h4>
				<div class="table-responsive">
					{% if options == NULL %}
						<h4 class="text-center">
							<strong>Aucune option pour le moment.</strong>
						</h4>
					{% else %}
						<table id="example" class="table table-hover">
							<thead>
								<tr>
									<th>Options</th>
									<th>Actions</th>
								</tr>
							</thead>
							<tbody>
								{% for option in options %}
									<tr>
										<td>
											<strong>{{ option.nom }}</strong>
										</td>
										<td>
											<a href="{{ path("app_user_document_annee_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id, "idOption": option.id}) }}" class="btn btn-outline-primary">Années</a>
										</td>
									</tr>
								{% endfor %}
							</tbody>
						{% endif %}
					</table>
				</div>
			</div>
		</div>
	</div> #}

	<div class="card">
		<div class="card-header d-flex justify-content-between align-items-center">
			<h4 class="card-title">Liste des options
			</h4>
			<div class="d-flex ">
      <a href="{{ path("app_user_document_licence_liste", {"idDepartement": departement.id, "idFiliere": filiere.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un option">
        <i class="mdi mdi-reply-all  "></i>Retour
      </a>
			</div>
		</div>
		<div class="card-body px-0 pb-0">
			<div class="table-responsive">
				<table class='table mb-0' id="table1">
					{% if options == NULL %}
						<h5 class="text-center">
							<strong>Aucune option pour le moment.</strong>
						</h5>
					{% else %}
						<thead>
							<tr>
								<th>Options</th>
								<th>Actions</th>
							</tr>
						</thead>
						<tbody>
							<tr>
								{% for option in options %}
									<tr>
										<td>
											<strong>{{ option.nom }}</strong>
										</td>
										<td>
											{# <a href="{{ path("app_admin_option_detail", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id, "idOption": option.id}) }}" title="Cliquez ici pour voir les détails cette option" class="btn btn-outline-dark">Détails</a> #}
											<a href="{{ path("app_user_document_annee_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id, "idOption": option.id}) }}" class="btn btn-outline-primary">Années</a>
										</td>
									</tr>
								{% endfor %}
							</tr>
						</tbody>
					{% endif %}
				</table>
			</div>
		</div>
	</div>

{% endblock %}
