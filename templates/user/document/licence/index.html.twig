{% extends 'base.user.html.twig' %}

{% block title %}
	Liste des licences {{ filiere.nom }}
{% endblock %}

{% block titre %}
	Liste des licences <strong>{{ filiere.nom }}</strong>
{% endblock %}

{% block description %}
	<a href="{{ path("app_user_document_filiere_liste", {"idDepartement": departement.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un licence">
		<i class="mdi mdi-reply-all  "></i>Retour
    </a>
{% endblock %}

{% block body %}
	
            {# <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <div class="table-responsive">
                  {% if licences == NULL %}
                    <h4 class="text-center"><strong>Aucune filière pour le moment.</strong></h4>
                  {% else %}
                  <table id="example" class="table table-hover">
                      <thead>
                        <tr>
                          <th>Licences</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                      {% for licence in licences %}
                         <tr>
                          <td><strong>{{ licence.nom }}</strong></td>
                          <td>
                            <a href="{{ path("app_user_document_option_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id}) }}" class="btn btn-outline-primary">Options</a>
                          </td>
                        </tr>
                      {% endfor %}
                      </tbody>
                  {% endif %}
                    </table>
                  </div>
                </div>
              </div>
            </div> #}


    <div class="card">
		<div class="card-header d-flex justify-content-between align-items-center">
			<h4 class="card-title">Liste des Licences
			</h4>
			<div class="d-flex ">
        <a href="{{ path("app_user_document_filiere_liste", {"idDepartement": departement.id}) }}" class="btn btn-outline-primary align-items-center" title="Cliquez ici pour ajouter un licence">
          <i class="mdi mdi-reply-all  "></i>Retour
        </a>
			</div>
		</div>
		<div class="card-body px-0 pb-0">
			<div class="table-responsive">
				<table class='table mb-0' id="table1">
	        {% if licences == NULL %}
                    <h4 class="text-center"><strong>Aucune filière pour le moment.</strong></h4>
                  {% else %}
					<thead>
						<tr>
							<th>Filières</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						<tr>
							{% for licence in licences %}
                         <tr>
                          <td><strong>{{ licence.nom }}</strong></td>
                          <td>
                            <a href="{{ path("app_user_document_option_liste", {"idDepartement": departement.id, "idFiliere": filiere.id, "idLicence": licence.id}) }}" class="btn btn-outline-primary">Options</a>
                          </td>
                        </tr>
                      {% endfor %}
						</tr>
					</tbody>
	        {% endif %}
				</table>
			</div>
		</div>
	</div>
{% endblock %}
